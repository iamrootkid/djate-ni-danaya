
[build]
  command = "npm run build"
  publish = "dist"
  base = "/"

# Handle client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"

[build.processing]
  skip_processing = false
